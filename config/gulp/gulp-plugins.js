// Импортируем модули
import notify from 'gulp-notify';
import plumber from 'gulp-plumber';
import flatten from 'gulp-flatten';

// Экспортируем объект
const gulpPlugins = {
	notify,
	plumber,
	flatten,
};

export default gulpPlugins;
